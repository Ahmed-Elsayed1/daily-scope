name: common
description: Common design system and shared UI components for Daily Scope
publish_to: 'none'
version: 1.0.0

resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^4.0.0
