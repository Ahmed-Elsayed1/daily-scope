name: weather_module
publish_to: "none"
description: Weather feature module powered by OpenWeather APIs.
version: 0.1.0

# resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.4
  freezed_annotation: ^2.4.1
  http: ^1.2.1
  sqflite: ^2.3.3
  path: ^1.9.0

dev_dependencies:
  flutter_lints: ^4.0.0
  build_runner: ^2.4.9
  freezed: ^2.5.2
