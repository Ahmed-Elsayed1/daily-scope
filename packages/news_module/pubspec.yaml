name: news_module
publish_to: "none"
description: News feature module built on NewsAPI + SQLite cache.
version: 0.1.0

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.4
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  http: ^1.2.1
  sqflite: ^2.3.3
  path: ^1.9.0
  intl: ^0.19.0
  webview_flutter: ^4.4.2
  connectivity_plus: ^6.0.3
  common:
    path: ../common

dev_dependencies:
  flutter_lints: ^4.0.0
  build_runner: ^2.4.9
  freezed: ^2.5.2
  json_serializable: ^6.7.1
